# Normalize line endings to LF in the repo; checkout as-is on all platforms.
* text=auto eol=lf

# Treat binary assets as binary (no line-ending conversion, no diff).
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff2 binary
*.woff binary
